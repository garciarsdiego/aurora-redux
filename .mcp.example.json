{
  "_note": "Gateway-free setup: the legacy HTTP gateway on port 20228 is disabled and its MCP server was removed from this template. Model routing is now by model-id prefix (kimi/ minimax/ glm/ = direct HTTP; claude-cli/ codex-cli/ = local CLI). See README.md and docs/USAGE.md. The omniforge (stdio) server below works with no extra configuration.",
  "mcpServers": {
    "omniforge": {
      "type": "stdio",
      "command": "node",
      "args": [
        "./bin/omniforge",
        "mcp-server"
      ],
      "env": {
        "DOTENV_CONFIG_QUIET": "true"
      }
    }
  }
}
