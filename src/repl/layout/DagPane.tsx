// DagPane — árvore de tasks colorida por status; auto-scroll para task ativa.
// Implementation phase: MD.
import React from 'react';
import { Box, Text } from 'ink';

export function DagPane(): React.ReactElement {
  return (
    <Box>
      <Text dimColor>(DAG pane placeholder)</Text>
    </Box>
  );
}
