// GatesQueue — lista lateral FIFO de hitl_gates pending.
// FIFO por gate timestamp (Example decision 2026-04-23). Ctrl+G overlay para skip.
// Implementation phase: MD.
import React from 'react';
import { Box, Text } from 'ink';

export function GatesQueue(): React.ReactElement {
  return (
    <Box>
      <Text dimColor>(gates queue placeholder)</Text>
    </Box>
  );
}
