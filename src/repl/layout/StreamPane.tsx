// StreamPane — token streaming render via useSyncExternalStore.
// Subscribes to TokenBuffer (singleton FORA do Zustand — D-H2.029).
// Implementation phase: MC.
import React from 'react';
import { Box, Text } from 'ink';

export function StreamPane(): React.ReactElement {
  return (
    <Box>
      <Text dimColor>(stream pane placeholder)</Text>
    </Box>
  );
}
